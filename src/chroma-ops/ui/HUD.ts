export class HUD { constructor(private el:HTMLElement){} render(d:{hp:number;ammo:number;reserve:number;label:string;notice:string;kills:number;deaths:number;dead:boolean;killer:string;hit:boolean;kill:boolean;reloading:boolean}){this.el.innerHTML=`<div class="co-score">KILLS ${d.kills} <span>／</span> DEATHS ${d.deaths}</div><div class="co-hp">HP <b>${d.hp}</b></div><div class="co-ammo">${d.label}<br><b>${d.ammo} <small>/ ${d.reserve}</small></b>${d.reloading?'<em>RELOADING</em>':''}</div>${d.notice?`<div class="co-notice">${d.notice}</div>`:''}<i class="co-crosshair ${d.kill?'is-kill':''}"></i>${d.hit?'<i class="co-hit">×</i>':''}${d.dead?`<div class="co-dead">YOU DIED<small>Killed by ${d.killer}<br>Respawning…</small></div>`:''}`;} }
