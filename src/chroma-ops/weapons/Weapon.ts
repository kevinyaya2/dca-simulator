export type WeaponSpec={id:string;label:string;magazine:number;reserve:number;damage:number;fireDelay:number;reloadTime:number;spread:number;recoil:number}; export type ShotResult={fired:boolean;empty:boolean;autoReload:boolean}; export abstract class Weapon { abstract readonly spec:WeaponSpec;abstract update(dt:number,shoot:boolean):ShotResult;abstract reload():void;abstract addReserve(amount:number):void;abstract get ammo():number;abstract get reserve():number;abstract get reloading():boolean; }
